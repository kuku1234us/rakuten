"use client";

// We're no longer using this custom wrapper since we're using the direct next-intl imports
// Re-export for backwards compatibility but this file isn't actively used
export { useTranslations } from "next-intl";
